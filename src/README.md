# src

Firmware source tree. `main.cpp` is the composition root; each subdirectory is a module with its own `CLAUDE.md`. This root level covers only cross-cutting files (Globals, AppContent, Functions).

See [CLAUDE.md](CLAUDE.md) for the full AI reference (Module Doc Map, cross-cutting files, display policies).
