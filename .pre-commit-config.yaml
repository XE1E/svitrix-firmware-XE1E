repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: '^lib/'
      - id: end-of-file-fixer
        exclude: '^lib/'
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=500']
        exclude: '^docs/assets/.*\.gif$'
      - id: check-merge-conflict

  - repo: https://github.com/compilerla/conventional-pre-commit
    rev: v3.6.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]
        args:
          - feat
          - fix
          - refactor
          - perf
          - docs
          - test
          - chore
          - build
          - ci
          - style
          - release

  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v18.1.8
    hooks:
      - id: clang-format
        files: '^src/.*\.(cpp|h)$'
        args: ['--style=file']

  - repo: https://github.com/pocc/pre-commit-hooks
    rev: v1.3.5
    hooks:
      - id: cppcheck
        args:
          - '--language=c++'
          - '--suppress=missingInclude'
          - '--suppress=unusedFunction'
          - '--suppress=unmatchedSuppression'
          - '--suppress=toomanyconfigs'
          - '--suppress=unknownMacro'
          - '--suppress=uninitMemberVar'
          - '--suppress=uninitMemberVarPrivate'
          - '--suppress=unusedStructMember'
          - '--suppress=duplicateCondition'
          # warning,performance only (NOT style) to match CI
          # (.github/workflows/ci.yml) — avoids blocking on style nits / false
          # positives (e.g. ArduinoJson's `x | default`) that CI never checks.
          - '--enable=warning,performance'
          - '--inline-suppr'
          - '--std=c++17'
        files: '^src/.*\.(cpp|h)$'
