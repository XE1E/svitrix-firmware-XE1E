# config

Shared configuration structs consumed by every module. Single header, no logic, no dependencies beyond Arduino `String`.

See [CLAUDE.md](CLAUDE.md) for the full reference (config structs, defaults, NVS persistence, change-impact map).
